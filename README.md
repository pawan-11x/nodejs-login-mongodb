# Node.js Login Form with MongoDB

This project stores login data into MongoDB using Node.js and Express.
To use the login form type http://localhost:5000/login_form.html to the browser.
when the user hit the Login button in the form , The enter by the user will move to the database.
